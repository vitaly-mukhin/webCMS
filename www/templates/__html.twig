<!DOCTYPE html>
<html>
	<meta charset="utf-8"/>
    <head>
        {{ head | default('<!-- here should be the Block\Head -->') | raw }}
    </head>
    <body>
        {{ body | default('<!-- here should be the Block\Head -->') | raw }}
    </body>
</html>
