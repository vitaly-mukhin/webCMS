<!DOCTYPE html>
<html>
	<head>
		{{ Block_Flow_Head | default('<!-- here should be the Block_Flow_Head -->') | raw }}
		</head>
		<body>
			<div class="container">
				<div class="navbar navbar-fixed-top">
					<div class="navbar-inner">
						<div class="container-fluid">
							<a class="logo pull-left"><img src="/////img/logo.png" alt="Main Logo"/></a>
							<div class="pull-right">
								<div class="row-fluid">
									{{ Block_Flow_Nav | default('<!-- here should be the Block_Flow_Nav -->') | raw }}
									</div>
									<div class="row-fluid">
										{{ Block_Flow_Login | default('<!-- here should be the Block_Flow_Login -->') | raw }}
										</div>
									</div>
								</div>
							</div>
						</div>
			{% block head %}
						<h1>Hello, World!</h1>
			{% endblock %}
						<div id="body-content">
							{% block content %}{% endblock %}
						</div>
					</div>
				</body>
			</html>
